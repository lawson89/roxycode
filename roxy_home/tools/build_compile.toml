description = "Runs a Maven clean compile to check for syntax errors and build the project. Returns the build log."
type = "graaljs"
source = "file:build_compile.js"

[[parameters]]
name = "dummy"
type = "string"
description = "Unused parameter required by schema parser"
required = false