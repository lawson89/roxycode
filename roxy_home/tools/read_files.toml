description = "Reads the content of multiple files from the project."
type = "groovy"
source = "file:read_files.groovy"

[[parameters]]
name = "paths"
type = "array"
description = "A list of relative paths to the files to read."
required = true
