description = "Deletes a file or directory (recursively)."
type = "groovy"
source = "file:delete_file.groovy"

[[parameters]]
name = "path"
type = "string"
description = "The path to delete."
required = true
