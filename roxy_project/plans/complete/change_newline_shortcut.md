
# Plan: Change newline shortcut to Shift-Enter

Change the keyboard shortcut for inserting a newline in the chat input field from Alt-Enter to Shift-Enter.

## Checklist
- [x] Modify `ChatView.java` to map `shift ENTER` to `insert-break`.
- [x] Remove or replace `alt ENTER` mapping.
- [x] Verify the change.

## Implementation Progress
- [x] Update `ChatView.java`
